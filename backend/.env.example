# Spring profile selection
# Set to "local" for MySQL (development) or "cloud" for Supabase Postgres
SPRING_PROFILES_ACTIVE=local

# Common server settings
PORT=5050

# Local profile (MySQL) variables
# When SPRING_PROFILES_ACTIVE=local, these are used
DATASOURCE_URL=jdbc:mysql://localhost:3306/inventory
DATASOURCE_USER=inventory
DATASOURCE_PASSWORD=123456789

# Cloud profile (Supabase Postgres) variables
# When SPRING_PROFILES_ACTIVE=cloud, these are used
# Replace placeholders with your Supabase details
SUPABASE_DB_URL=jdbc:postgresql://YOUR_DB_HOST:5432/postgres?sslmode=require
SUPABASE_DB_USER=YOUR_DB_USER
SUPABASE_DB_PASSWORD=YOUR_DB_PASSWORD

# Notes:
# - Avoid embedding special characters directly in JDBC URLs; keep username/password separate.
# - Ensure `sslmode=require` for Supabase connections.
# - Copy this file to `.env` and adjust values for your environment.
